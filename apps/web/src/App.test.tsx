import{fireEvent,render,screen}from'@testing-library/react';import{describe,expect,it}from'vitest';import{App}from'./App';
describe('Traefik Pilot UI',()=>{it('renders the infrastructure overview and simple admin actions',()=>{render(<App/>);expect(screen.getByRole('heading',{name:'Infrastructure overview'})).toBeInTheDocument();expect(screen.getByText('Route health')).toBeInTheDocument();expect(screen.getByRole('region',{name:'Common admin actions'})).toBeInTheDocument()});it('runs the quick health check',()=>{render(<App/>);fireEvent.click(screen.getByRole('button',{name:/run health check/i}));expect(screen.getByText('All systems checked')).toBeInTheDocument()});it('opens and advances the route wizard',()=>{render(<App/>);fireEvent.click(screen.getAllByRole('button',{name:/create route/i})[0]!);expect(screen.getByRole('heading',{name:'Select a container'})).toBeInTheDocument();fireEvent.click(screen.getByRole('button',{name:/continue/i}));expect(screen.getByRole('heading',{name:'Select the target port'})).toBeInTheDocument()});it('navigates to container controls',()=>{render(<App/>);fireEvent.click(screen.getByRole('button',{name:'Containers'}));expect(screen.getByRole('heading',{name:/Docker containers/})).toBeInTheDocument()})});
