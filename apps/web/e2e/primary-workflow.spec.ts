import{test,expect}from'@playwright/test';
test('create and validate a route',async({page})=>{await page.goto('/');await page.getByRole('button',{name:/create route/i}).click();for(let i=0;i<7;i++)await page.getByRole('button',{name:/continue/i}).click();await expect(page.getByText('Syntax valid')).toBeVisible();await page.getByRole('button',{name:/continue/i}).click();await expect(page.getByText('Safe change sequence')).toBeVisible()});
test('emergency container control is reachable',async({page})=>{await page.goto('/');if((page.viewportSize()?.width||1280)<780)await page.getByRole('button',{name:'Open navigation',exact:true}).first().click();await page.getByRole('button',{name:'Containers',exact:true}).click();await expect(page.getByRole('heading',{name:/Docker containers/})).toBeVisible();await expect(page.getByRole('button',{name:/restart/i}).first()).toBeVisible()});
